// import { useQuery } from "@tanstack/react-query";
// import { tasksNetwork } from "../../lib/networks/tasks";

export const ListTasksPage = () => {
  // const tasksQuery = useQuery({
  //   queryKey: ["task", "list"],
  //   // queryFn: tasksNetwork.list,
  // })
  return <p>gmm</p>;
};
